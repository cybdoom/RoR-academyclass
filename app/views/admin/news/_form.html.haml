= semantic_form_for [:admin, @news], :html => {:multipart => true} do |p|  
  = p.inputs do
    = p.input :title
    .no-float-label
      = p.input :description, input_html: {class: "basic-input ckeditor"}
    .no-float-label
      = p.input :content, input_html: {class: "basic-input ckeditor"}
    = p.input :sticky
    = p.input :image
  = p.actions
