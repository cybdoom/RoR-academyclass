- content_for :breadcrumbs do
  = link_to "Home", root_path
  = breadcrumb_link_to "News", news_index_path
  = breadcrumb_link_to @news.title, news_path(@news)
- content_for :meta_title do
  Academy Class - News - #{@news.title}

.news
  %h2= @news.title
  .published Published on #{news_date_val @news.published_at}
  - if @news.created_at < "2015-08-20"
    .news-img= image_tag @news.image.url(:large)
  .news-content= raw @news.content
  .clearfix