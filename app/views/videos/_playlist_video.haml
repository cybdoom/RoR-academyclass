%li
  = link_to image_tag(playlist_video.thumbnail, :alt => playlist_video.title), full_video_path(playlist_video)
  %p= link_to playlist_video.title, full_video_path(playlist_video)
  .member-only-overlay
  .member-only-text Only for members
